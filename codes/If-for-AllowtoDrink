#include <stdio.h>

    int age, i;
    
    int main() {
    
        printf("Enter your age:");
        scanf("%d", &age);
    
        if (age >= 18)
            for(i=1; i<=age; i++){
            printf("You're allowed to drink\n");
            }
    
    return 0;
    }
    
    